using GameApp;
using Game.Frame.UI;
using Game.Frame.UI.Core;
using Game.Extern.QuickUI;
using UnityEngine.UI;

namespace GameApp.UI.{0}
{{
    [UIWindow("{1}", LayerType.Form, WindowMode.Exclusive, ClearMode.Default)]
    public partial class {2} : UIWindow
    {{
        #region (禁止编辑)自动生成变量
        // HashCode:0
        #endregion (禁止编辑)自动生成变量

        private void InitComponents()
        {{
            QuickUIMono quickUI = RectTransform.GetComponent<QuickUIMono>();
            /// 在这里初始化组件

            #region (禁止编辑)自动生成绑定

            #endregion (禁止编辑)自动生成绑定
        }}
    }}
}} 